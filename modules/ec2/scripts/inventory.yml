all:
  children:
    docker:
      hosts:
        10.0.1.29
  vars:
    ansible_user: ubuntu
    ansible_ssh_private_key_file: /home/ubuntu/.ssh/Estio-Ubuntu.pem
  
    ansible_ssh_common_args: "-o StrictHostKeyChecking=no"
